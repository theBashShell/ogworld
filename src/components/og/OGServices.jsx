import React from "react";

const OGServices = ({ img, head, text }) => {
  return (
    <div>
      <div></div>
      <div>{head}</div>
      <div>{text}</div>
    </div>
  );
};

export default OGServices;
